# Internal email domain; attendees with this domain are not treated as "external".
# Leave empty to treat all attendees as external.
INTERNAL_DOMAIN=anysphere.co

# Optional: timezone for building day bounds (default: America/Los_Angeles).
# TZ=America/New_York
